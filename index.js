/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Kalkulator from './Kalkulator';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Kalkulator);
